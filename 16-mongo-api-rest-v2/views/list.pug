extends layout

block content
  h1= title
  h2 Listado
  p Welcome to #{title}
  include includes/menu.pug
  table(id="tabla",class="table")
    thead(class="")
        tr(class="")
            th(scope="col") Nombre
            th(scope="col") Acciones
    tbody
        
    