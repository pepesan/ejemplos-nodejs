extends layout

block content
  h1= title
  p Welcome to #{title}
  include includes/menu.pug
  div
    h1 #{item.username}
    h2 #{item.hash}
    a(class="btn btn-warning",id="modificar",href="/api/edita/"+item._id) Editar
    a(class="btn btn-danger",id="borrar",href="/api/borra/"+item._id) Borrar